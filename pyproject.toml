[project]
name = "lfm-label-free-meter"
version = "0.1.0"
description = "Label-Free Meter for AI state evaluation"
requires-python = ">=3.8"
dependencies = [
    "pandas",
    "pyarrow",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["LFM*"]
